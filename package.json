{
  "name": "deezer-cloudflare-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker proxy for Deezer gw-light.php API",
  "main": "src/index.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "@cloudflare/workers-types": "^4.20260425.1",
    "typescript": "^6.0.3",
    "vitest": "~3.2.0",
    "wrangler": "^4.85.0"
  }
}
